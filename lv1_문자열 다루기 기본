def solution(s):
    cnt = 0
    if s.isdigit() == True:
        cnt += 1
    if len(s) == 4 or len(s) == 6:
        cnt += 1
    if cnt == 2:
        return True
    else:
        return False

# best 
"""
def solution(s):
    return s.isdigit() and len(s) in [4, 6]
"""