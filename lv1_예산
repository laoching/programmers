def solution(d, budget):
    d.sort() # 1 2 3 4 5
    sum = 0
    cnt = 0

    for i in d:
        sum += i   
        if sum > budget:
            break    
        else:
            cnt += 1
            

    
    
    return cnt

print(solution([1,3,2,5,4], 9))