def solution(arr1, arr2):
    a = b = 0
    for i in range(len(arr1)):
        for j in range(len(arr2)):
           a += arr1[i][j] 
           b += arr2[i][j] 
                
    return [a,b]

print(solution([[1,2],[2,3]], [[3,4],[5,6]]))